var infoTplMarkup = [
  '<span style="font-size:12px"><b>Description</b>: {description}</style><br/>',
  '<span style="font-size:12px"><b>Access Code</b>: {access_code}</style><br/>',
];
var infoTpl = new Ext.Template(infoTplMarkup);

grid.getSelectionModel().on('rowselect', function(sm, rowIdx, r) {
    var infoPanel = Ext.getCmp('infoPanel');
    infoTpl.overwrite(infoPanel.body, r.data);
});

