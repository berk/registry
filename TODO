== Stuff to do

Add undo history

Prevent re-adding of already deleted keys

Add an updater method to the configuration which takes a proc

Add notifications
  listener thread

Add search box

Add lockability
  lock icon
